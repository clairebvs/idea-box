<%= @idea.title %><br>
<%= @idea.description %><br>
<% @idea.images.each do |image| %>
  <%= image.name %><br>
<% end %>

<%= link_to "Edit", edit_user_idea_path(@user, @idea) %>
<br>
<%= button_to 'Delete', user_idea_path(@user, @idea), method: :delete %> <br>
<%= link_to 'Add An Image', edit_user_idea_path(@user, @idea) %> <br>


<%= link_to 'Back to your ideas', user_ideas_path(@user) %>
